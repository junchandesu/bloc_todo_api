<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h1>Todo List: <%= @list.title %></h1>
			<%= render @list.items %>
			<br/>
			<br/>
		</div>

		<div class="col-md-10">
			<i>Let's add a new todo item.</i>
			<%= render partial: "items/newItems", locals: { list: @list, item: @list.items.build }  %>
		</div>

	</div>
</div>

