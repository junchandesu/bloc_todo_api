<div class="container">
	<h1> Things to do List </h1>
		<% @lists.each do |list| %>
			<p><h4><%= link_to list.title, list_path(list) %></h4></p>
		<% end %>
		<br/>
		<%= link_to "Add a new ToDo item.", new_list_path, class:"label label-warning" %>
</div>
